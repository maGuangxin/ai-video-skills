# sk6 输出模板

## 模板 1：sub-shot-<ID>.srt
> 编码建议：UTF-8。默认不带角色名前缀，除非项目配置明确开启。
> 每行不超过 12 个汉字（超长在生成时断行）；竖屏发布时字幕在剪辑阶段应放入安全区（见剪辑操作手册）。

```srt
1
00:00:02,500 --> 00:00:06,000
你可算来了，我等你快半小时了

2
00:00:06,500 --> 00:00:09,000
真的很抱歉路上堵车了
```

## 模板 2：tts-voiceover-full.md
| # | shot ID | 角色 | 情绪 | 台词原文 | 字数 | 建议语速 | 预计时长 | 建议音色 |
|---|---|---|---|---|---|---|---|---|
| 1 | shot-1A | | | | | | | |
| 2 | shot-1B | | | | | | | |

### TTS 建议参数
- 采样率：`<按平台填写>`
- 比特率：`<按平台填写>`
- 输出目录：`<项目自定>`
- 静音处理：`<按需要填写>`

## 模板 3：voice-strategy.md

# 音频路线说明

## 当前路线
- 音频路线：`<mute_plus_tts / native_audio / hybrid>`
- 选择原因：`<结合模型能力画像填写>`

## 镜头级建议
| shot ID | 建议路线 | 原因 | 备注 |
|---|---|---|---|
| shot-1A | | | |
| shot-1B | | | |

## 回退策略
1. `<何时回退到静音 + TTS>`
2. `<何时保留原生音频>`
3. `<何时采用混合方案>`

## 模板 4：sound-design.md

# BGM 与音效设计

## BGM 方向
- 情绪基调：`<轻快 / 紧张 / 抒情 / 悬疑…>`
- 参考节奏：`<BPM 区间>`
- 结构建议：`<进副歌 / 抽底鼓留白的时间点，结合情绪节拍表>`
- 版权提示：建议使用发布平台曲库或免版权音乐

## 每镜头音效建议
| shot ID | 节拍点 | 音效类型 | 说明 |
|---|---|---|---|
| shot-1A | `<节拍切换时间>` | `<转场whoosh / 强调hit / 环境底噪 / 特殊音效 / 无>` | `<与画面动作的对应关系>` |
| shot-1B | | | |

## 卡点说明
- 节拍切换点优先落在音效重音：`<具体时间点列表>`
- 爆点节拍必配强调类音效：`<shot ID 与音效>`
- 节拍表状态：`<已按情绪节拍表卡点 / 节拍表缺失，按镜头边界对齐（卡点未校验）>`

## 混音层级建议
- 默认层级：台词 > 音效 > BGM；`<项目特殊要求>`

## 模板 5：jianying-step-by-step.md

# 剪映操作说明

### 第 1 步：新建项目并导入素材
`<说明>`

### 第 2 步：检查接缝与黑屏
`<说明>`

### 第 3 步：导入字幕
`<说明>`
> 竖屏发布时：字幕放置于画面下方 10%–22% 高度安全区，避开平台 UI；每行不超过 12 字；每条最多高亮 1–2 个关键词（钩子与爆点台词优先）；具体字体颜色按美术风格建议值填写。

### 第 4 步：导入配音或保留原生音频
`<说明>`

### 第 5 步：处理背景音乐与音效（可选）
`<说明>`
> 按 sound-design.md 的每镜头音效建议与卡点说明添加音效；混音层级保持 台词 > 音效 > BGM。

### 第 6 步：整体预览
`<说明>`

### 第 7 步：导出成片
`<说明>`
