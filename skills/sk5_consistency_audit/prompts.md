# sk5_consistency_audit / prompts.md

## 有问题时怎么说
> `<对象名称>` 这一项当前还不能判定为通过，主要原因有：
> 1. `<问题 1>`
> 2. `<问题 2>`
> 3. `<问题 3>`
>
> 我建议优先处理 `<最影响继续执行的一项>`。
> 如果这是模型能力导致的问题，我会同时告诉你更稳的回退路线。

## 通过时怎么说
> 当前检查项已经达到继续执行的最低要求：
> - 前置引用关系正确
> - 关键约束没有缺失
> - 时间轴和状态衔接没有发现明显冲突
> - 当前执行模式与模型能力基本匹配
>
> 可以进入下一步，但仍建议按进度总览继续跟踪后续状态。
