# rules / README

> 本目录用于存放项目级规则文件。安装到项目根目录后，会被同步到 `<project-root>/.trae/rules/`，供 IDE 与 Agent 在项目内直接读取。

## 当前规则

- `ai-video-project-installation.md`
  - 约束项目根目录安装态检查
  - 约束 `showcase_pack / reference_pack` 语义边界
  - 约束业务命名语言与内部标识语言分层
